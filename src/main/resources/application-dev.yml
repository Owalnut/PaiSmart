server:
  port: 8081 # 指定端口号
spring:
  datasource:
    url: jdbc:mysql://localhost:3306/PaiSmart?useSSL=false&serverTimezone=UTC
    username: root
    password: 20041027Djs
    driver-class-name: com.mysql.cj.jdbc.Driver
  data:
    redis:
      host: localhost
      port: 6379
  servlet:
    multipart:
      enabled: true
      max-file-size: 50MB # 单个文件的最大大小
      max-request-size: 100MB # 整个请求的最大大小
  rabbitmq:
    host: 127.0.0.1
    port: 5672
    virtual-host: /pai_coding
    username: walnut
    password: 123456
    template:
      retry:
        enabled: true
  webflux:
    client:
      max-in-memory-size: 16MB  # 增加响应大小限制
  codec:
    max-in-memory-size: 16MB    # 增加编解码大小限制


minio:
    endpoint: http://localhost:9000
    accessKey: admin
    secretKey: admin123
    bucketName: uploads
    publicUrl: http://localhost:9000
jwt:
  secret-key: "PXrQbuCwXwOZzkML/Vm2S5rSwt1iybvmKtGDzVEu+Hc="

# 管理员账号初始化配置
admin:
  username: admin
  password: admin123
  primary-org: default
  org-tags: default,admin

file:
  parsing:
    chunk-size: 512 # 每个文本块的最大字符数

elasticsearch:
  host: localhost       # Elasticsearch主机地址
  port: 9200            # Elasticsearch端口号
  scheme: http          # 协议（http/https）

logging:
  level:
    org.springframework.web: DEBUG
    org.springframework.boot.autoconfigure.web.servlet: DEBUG
    org.springframework.security: DEBUG
    com.yizhaoqi.smartpai.service: DEBUG
    io.minio: DEBUG

log4j:
  logger:
    org:
      apache:
        tika=DEBUG:

deepseek:
  api:
    url: https://api.deepseek.com/v1  # 本地是 http://localhost:11434/v1 官方：https://api.deepseek.com/v1
    model: deepseek-chat  # 本地: deepseek-r1:7b, 官方: deepseek-chat
    key: sk-26a06d3bee354fadbf1fce03c649586f # DeepSeek API Key 如果是本地就为空

embedding:
  api:
    url: https://dashscope.aliyuncs.com/compatible-mode/v1
    key: sk-54e1791db99a49fe991fbd8b5a2065bd   # 直接写入火山引擎的 API key
    model: text-embedding-v4
    dimension: 2048